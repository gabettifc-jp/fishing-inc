# チェック機構の型

自動はスクリプト一本だけ。Claudeを走らせる自動チェックは置かない。

## 自動（トークンを使わない）

`scripts/check.js` を用意し、`src/` を変えたら走らせる。
中身は機械で確かめられるものだけ。

- つまみの一覧と、実際に画面から動かせる数が一致するか
- 定数がファイルの先頭にまとまっているか
- 触るなと決めた箇所が変わっていないか
- 仕様書の版と、実装が参照した版が一致するか

> **`sheets/` の版は見ない。**シートは**索引**で、値を持っていないので古くならない
> （`gamedev/templates/sheets/README.md`「シートは索引であって、答えの置き場ではない」）。
> **代わりに、行き先が無い問いと根拠が空の問いを数える。**
> 版を照合しても「合わない」としか出ないが、こちらは**どの問いが落ちたか**が出る。

**いま在るもの。**

| 走らせるもの | 何を見るか |
|---|---|
| `node tools/smoke.mjs` | 読み込んで、押して、しばらく回してエラーが出ないか。画面を1枚出す |
| `node tools/friction-check.mjs` | **`gamedev/friction.md` の翻訳に添えた「測り方」を実際に測る。**<br>機械で測れないものは「測れない」と出す（**黙って飛ばさない**） |
| `node ../gamedev/tools/check-sheets.mjs sheets` | **`sheets/` のうち、行き先が仕様書に無い問いと、根拠が空の問いを数える。**<br>**空いていること自体は誤りではないので、落とさない。**<br>誤りなのは、空いているのに気づかないまま先へ進むこと |

> **失敗は、終了コードと最後の行に必ず出す。**
> 2026-08-20、`smoke.mjs` は「魚が掛かると落ちる」を**ちゃんと捕まえていた。**
> なのに見落とした。`node tools/smoke.mjs | tail -1` で見ていて、
> 最後の行が「画面を保存した」だったからである。
> **出力を切り詰めて見るのは避けられないので、道具の側で最後に出す。**

> **検査は「本物と同じ呼ばれ方」で呼ぶ。**
> 同じ日、旋律の検査は `melodyOf([...c])`（配列）で呼んでいたが、
> **本物は文字列で呼んでいた。**文字列に `.map` は無いので、実物だけが落ちた。
> 検査の中で引数を作り直すと、**作り直した形しか試していない**ことになる。

> **表が無いと、抜けは見えない。**
> 2026-08-21、`02-meta-loop.md` のメタ2の層は**根拠が一つも書かれていなかった。**
> 第一段で埋めたときも、第二段で遡って根拠を付けたときも飛ばしている。
> **根拠を表の外（引用）に書いていたので、「引用が無い表」に気づけなかった。**
> 三列の表に直したら、その場で 6 と出た。

> **`friction-check.mjs` は、直したことが崩れていないかを見る道具でもある。**
> つまらなさの生ログ → 翻訳 → 測り方 → **ここ**、と繋がっている。
> 翻訳に測り方を書いたら、測れるものはここに足す。

## 手動（合図で呼ぶ）

- **「監査して」** ＝ spec.md だけを読み、曖昧・矛盾・未決を指摘する。実装しない。
  spec.md を大きく書き換えた直後に一回
- **「検算して」** ＝ spec.md と実装を突き合わせる。実装したセッションとは別で。
  人間が触って判定する直前に必ず一回

## 自己提案

CLAUDE.md の一行（仕様書に無いことを三つ以上決めることになったら止める）だけ。
条件を増やさない。増やすと効かなくなる。

## 一回の山につき

監査1回・検算1回。それ以外はスクリプトだけ。

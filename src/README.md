# src/

実装を置く。単一のHTMLファイルで作る。外部依存なし、絵と音はコードで作る。

## 中身

- `fishing-inc.html` … 第一段。仕様書第17版の 0〜12章。
  画面A（釣り・道具屋）／画面B（転生・パーク29個）／画面C（釣り場選び）／図鑑、
  10章のつまみ152個、測る仕組み（画面表示とTSV／JSON書き出し）、10.5章のセーブ。

## 決まり

- 数値はファイル先頭の `TUNE_DEF` に集約する。コードに直接書かない
- 触って判定した版は `builds/` にコピーで残す。`src/` は直し続けてよい

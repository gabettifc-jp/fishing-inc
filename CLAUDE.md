# CLAUDE.md（fishing-inc）

**共通の作法は gamedev リポジトリの CLAUDE.md を見よ**（https://github.com/gabettifc-jp/gamedev/blob/main/CLAUDE.md）。
ここにはこのゲーム固有の作法だけを書く。

## このゲームの正本

- `spec.md` が正本である。いまは第19版
- 面白さの核と仮説は 0章・1章に入っている
- 画面A・B・C・図鑑の設計は9章、つまみの初期値は10章、絵と音の方針は11章に入っている

## 落としてはいけない規則

仕様書で**決**が付いているもののうち、実装で崩れやすいもの。

- **符号の上限は8。テンポは速くしない。**深い釣り場でも拍は同じ。
  難しさを上げる代わりに、腕の差は「完璧を出せるかどうか」に出す
- **符号が示すのは格まで。**どの魚かは上がるまで分からない。超大物だけ譜面を固定する
- **自動は手動を上回らない。**自動はそこそこの精度どまりで、連続ボーナスを繋げられない
- **予兆にガセを入れない。**予兆が出たら必ず超大物
- **深場の渋さはリールの判定幅で作る。**絶対に不可能にはしない。腕でごり押しできる余地を残す
- **演出1（魚が上がった）は絶対に止めない。**通しで千回近く来るので、0.2秒でも合計で数十秒を奪う
- **報酬の音は高い音。**低くて持続する音は予兆にだけ使う

## 待ち時間の落とし穴

秒あたりの期待収入は、待ち時間によらず一定にする。
ただし**餌の「掛かるまでの時間が短くなる」効果だけは例外**。
この規則をそのまま適用するとレア率も一緒に下がって効果がゼロになる。
**待ち時間だけ縮めてレア率は据え置き。**

## 数値

- **10章のつまみは一つ残らず画面から動かせるようにする。**コードに直接書かない
- 10章の値は全部が仮。動かすために置いてあるだけで、正しさは求めていない
- 調整は10章末尾の**「調整の順番」の上から**やる。一拍の長さと判定の幅が土台で、
  そこが決まらないと下が決まらない

## 作り方

- 実装は `src/` に置く
- 触って判定した版は `builds/` に `YYYY-MM-DD-（何の版か）` で残す。上書きしない

## 決めた点の行き先

- 仕様書に無いのに決めざるを得なかった点は `open.md` に書く
- 決まったことは `decisions.md` に一件四行で書く
- 仮説と測り方は `hypotheses.md` に書く
- つまらなさを感じたら、その場で gamedev の `friction.md` に生のまま書く
